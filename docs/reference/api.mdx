---
title: API
---

On many platforms an alphaTab API object can be used to bootstrap alphaTab within your UI. It takes over 
all responsibilities of linking your actions to the UI. Namely this object is called `AlphaTabApi` (Web) or `AlphaTabApiBase<>` (.net).
This page describes all methods, properties and events exposed by this API object. 

import ReferenceTable from "./_apiTable.mdx";

<ReferenceTable />