---
title: activeBeatsChanged
description: This event is fired when the currently active beats across all tracks change.
sidebar_custom_props:
  category: Events - Player
  since: 1.2.3
---
import { SinceBadge } from '@site/src/components/SinceBadge';

<SinceBadge since="1.2.3" />

import { EventDescription } from '@site/src/components/EventDescription';
import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
This event is fired when the currently active beats across all tracks change. Unlike the [`playedBeatChanged`](/docs/reference/plain/alphatabapibase.mdx#playedbeatchanged) event this event contains the beats of all tracks and voices independent of them being rendered.

<EventDescription />

## Types

<TypeTable>
    <TypeRow type="js" name="function (args: alphaTab.synth.ActiveBeatsChangedEventArgs): void" />
    <TypeRow type="net" name="System.Action<AlphaTab.Synth.ActiveBeatsChangedEventArgs>" />
    <TypeRow type="android" name="(args: alphaTab.synth.ActiveBeatsChangedEventArgs) -> Unit" />
</TypeTable>

## Parameters

<ParameterTable>
    <ParameterRow platform="js" name="args" type="alphaTab.synth.ActiveBeatsChangedEventArgs">
        The event arguments containing the information for the event.
    </ParameterRow>
    <ParameterRow platform="net" name="args" type="AlphaTab.Synth.ActiveBeatsChangedEventArgs">
        The event arguments containing the information for the event.
    </ParameterRow>
    <ParameterRow platform="android" name="args" type="alphaTab.synth.ActiveBeatsChangedEventArgs">
        The event arguments containing the information for the event.
    </ParameterRow>
</ParameterTable>

### [alphaTab.synth.ActiveBeatsChangedEventArgs](/docs/reference/plain/activebeatschangedeventargs.mdx) Properties

Represents the information related to the beats actively being played now.

<ParameterTable>
    <ParameterRow platform="js" name="activeBeats" type="alphaTab.model.Beat[]">
        The currently active beats across all tracks and voices.
    </ParameterRow>
    <ParameterRow platform="net" name="ActiveBeats" type="IList<AlphaTab.Model.Beat>">
        The currently active beats across all tracks and voices.
    </ParameterRow>
    <ParameterRow platform="android" name="activeBeats" type="alphaTab.collections.List<alphaTab.model.Beat>">
        The currently active beats across all tracks and voices.
    </ParameterRow>
</ParameterTable>

## Examples

import ExampleTabs from "@theme/Tabs";
import ExampleTabItem from "@theme/TabItem";

<ExampleTabs
    defaultValue="javascript"
    values={[
      { label: "JavaScript", value: "javascript"},
      { label: "HTML", value: "html"},
      { label: "jQuery", value: "jquery"},
      { label: "C#", value: "csharp"},
      { label: "Android", value: "android"}
    ]}
>
<ExampleTabItem value="javascript">
```js
const api = new alphaTab.AlphaTabApi(document.querySelector('#alphaTab'));
api.activeBeatsChanged.on(args => {
   updateHighlights(args.activeBeats);
});
```
</ExampleTabItem>
<ExampleTabItem value="html">
```js
document.querySelector('#alphaTab').addEventListener('alphaTab.activeBeatsChanged', (e) => {
    const args = e.detail;
    updateHighlights(args.activeBeats);
}, false);
```
</ExampleTabItem>
<ExampleTabItem value="jquery">
```js
$('#alphaTab').on('alphaTab.activeBeatsChanged', (e, args) => {
 updateHighlights(args.activeBeats);
});
```
</ExampleTabItem>
<ExampleTabItem value="csharp">
```cs
var api = new AlphaTabApi<MyControl>(...);
api.ActiveBeatsChanged.On(args =>
{
    UpdateHighlights(args.ActiveBeats);
});
```
</ExampleTabItem>
<ExampleTabItem value="android">
```kotlin
val api = AlphaTabApi<MyControl>(...)
api.activeBeatsChanged.on { args ->
    updateHighlights(args.activeBeats)
}
```
</ExampleTabItem>
</ExampleTabs>
