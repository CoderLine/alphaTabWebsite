---
title: downloadMidi
description: Generates an SMF1.0 file and downloads it
sidebar_custom_props:
  category: Methods - Core
  since: 1.3.0
---
import { SinceBadge } from '@site/src/components/SinceBadge';

<SinceBadge since="1.3.0" />

import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
Generates an SMF1.0 file and downloads it Generates a SMF1.0 compliant MIDI file of the currently loaded song and starts the download of it.
Please be aware that SMF1.0 does not support bends per note which might result in wrong bend effects
in case multiple bends are applied on the same beat (e.g. two notes bending or vibrato + bends).

## Signatures

<TypeTable>
    <TypeRow type="js" name="downloadMidi(format?: MidiFileFormat): void;" />
    <TypeRow type="net" name="void DownloadMidi(AlphaTab.Midi.MidiFileFormat? format)" />
    <TypeRow type="android" name="fun downloadMidi(format: alphaTab.midi.MidiFileFormat?): Unit" />
</TypeTable>

### Parameters 

<ParameterTable>
    <ParameterRow platform="js" name="format" type="alphaTab.midi.MidiFileFormat?">
        (no description)
    </ParameterRow>
    <ParameterRow platform="net" name="format" type="AlphaTab.Midi.MidiFileFormat?">
        (no description)
    </ParameterRow>
    <ParameterRow platform="android" name="format" type="alphaTab.midi.MidiFileFormat?">
        (no description)
    </ParameterRow>
</ParameterTable>

### Returns 
Nothing 

## Example - JavaScript

```js
const api = new alphaTab.AlphaTabApi(document.querySelector('#alphaTab'));
api.downloadMidi();
```
