---
title: changeTrackVolume
description: Changes the volume of the given tracks.
sidebar_custom_props:
  category: Methods - Player
  since: 0.9.4
---
import { SinceBadge } from '@site/src/components/SinceBadge';

<SinceBadge since="0.9.4" />

import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
Changes the volume of the given tracks. This will result in a volume change of the primary and secondary midi channel that the track uses for playback.
If the track shares the channels with another track, all related tracks will be changed as they cannot be distinguished.

## Signatures

<TypeTable>
    <TypeRow type="js" name="changeTrackVolume(tracks: Track[], volume: number): void;" />
    <TypeRow type="net" name="void ChangeTrackVolume(IList<AlphaTab.Model.Track> tracks, double volume)" />
    <TypeRow type="android" name="fun changeTrackVolume(tracks: alphaTab.collections.List<alphaTab.model.Track>, volume: Double): Unit" />
</TypeTable>

### Parameters 

<ParameterTable>
    <ParameterRow platform="js" name="tracks" type="alphaTab.model.Track[]">
        The tracks for which the volume should be changed.
    </ParameterRow>
    <ParameterRow platform="net" name="tracks" type="IList<AlphaTab.Model.Track>">
        The tracks for which the volume should be changed.
    </ParameterRow>
    <ParameterRow platform="android" name="tracks" type="alphaTab.collections.List<alphaTab.model.Track>">
        The tracks for which the volume should be changed.
    </ParameterRow>
    <ParameterRow platform="js" name="volume" type="number">
        The volume to set for all tracks in percent (0-1)
    </ParameterRow>
    <ParameterRow platform="net" name="volume" type="double">
        The volume to set for all tracks in percent (0-1)
    </ParameterRow>
    <ParameterRow platform="android" name="volume" type="Double">
        The volume to set for all tracks in percent (0-1)
    </ParameterRow>
</ParameterTable>

### Returns 
Nothing 

## Examples

import ExampleTabs from "@theme/Tabs";
import ExampleTabItem from "@theme/TabItem";

<ExampleTabs
    defaultValue="javascript"
    values={[
      { label: "JavaScript", value: "javascript"},
      { label: "jQuery", value: "jquery"},
      { label: "C#", value: "csharp"},
      { label: "Android", value: "android"}
    ]}
>
<ExampleTabItem value="javascript">
```js
const api = new alphaTab.AlphaTabApi(document.querySelector('#alphaTab'));
api.changeTrackVolume([api.score.tracks[0], api.score.tracks[1]], 1.5);
api.changeTrackVolume([api.score.tracks[2]], 0.5);
```
</ExampleTabItem>
<ExampleTabItem value="jquery">
```js
const at = $('#alphaTab');
const tracks = at.alphaTab('tracks');
at.alphaTab('trackVolume', [tracks[0], tracks[1]], 1.5);
at.alphaTab('trackVolume', [tracks[0], tracks[1]], 0.5);
```
</ExampleTabItem>
<ExampleTabItem value="csharp">
```cs
var api = new AlphaTabApi<MyControl>(...);
api.ChangeTrackVolume(new Track[] { api.Score.Tracks[0], api.Score.Tracks[1] }, 1.5);
api.ChangeTrackVolume(new Track[] { api.Score.Tracks[2] }, 0.5);
```
</ExampleTabItem>
<ExampleTabItem value="android">
```kotlin
val api = AlphaTabApi<MyControl>(...);
api.changeTrackVolume(alphaTab.collections.List<alphaTab.model.Track>(api.score.tracks[0], api.score.tracks[1]), 1.5);
api.changeTrackVolume(alphaTab.collections.List<alphaTab.model.Track>(api.score.tracks[2]), 0.5);
```
</ExampleTabItem>
</ExampleTabs>
