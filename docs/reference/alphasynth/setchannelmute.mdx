---
title: setChannelMute
description: Sets the mute state of a channel.
sidebar_custom_props:
  category: Methods
---
import { SinceBadge } from '@site/src/components/SinceBadge';


import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
Sets the mute state of a channel. 

## Signatures

<TypeTable>
    <TypeRow type="js" name="setChannelMute(channel: number, mute: boolean): void;" />
    <TypeRow type="net" name="void SetChannelMute(double channel, bool mute)" />
    <TypeRow type="android" name="fun setChannelMute(channel: Double, mute: Boolean): Unit" />
</TypeTable>

### Parameters 

<ParameterTable>
    <ParameterRow platform="js" name="channel" type="number">
        The channel number
    </ParameterRow>
    <ParameterRow platform="net" name="channel" type="double">
        The channel number
    </ParameterRow>
    <ParameterRow platform="android" name="channel" type="Double">
        The channel number
    </ParameterRow>
    <ParameterRow platform="js" name="mute" type="boolean">
        true if the channel should be muted, otherwise false.
    </ParameterRow>
    <ParameterRow platform="net" name="mute" type="bool">
        true if the channel should be muted, otherwise false.
    </ParameterRow>
    <ParameterRow platform="android" name="mute" type="Boolean">
        true if the channel should be muted, otherwise false.
    </ParameterRow>
</ParameterTable>

### Returns 
Nothing 
