---
title: setChannelSolo
description: Gets the solo state of a channel.
sidebar_custom_props:
  category: Methods
---
import { SinceBadge } from '@site/src/components/SinceBadge';


import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
Gets the solo state of a channel. 

## Signatures

<TypeTable>
    <TypeRow type="js" name="setChannelSolo(channel: number, solo: boolean): void;" />
    <TypeRow type="net" name="void SetChannelSolo(double channel, bool solo)" />
    <TypeRow type="android" name="fun setChannelSolo(channel: Double, solo: Boolean): Unit" />
</TypeTable>

### Parameters 

<ParameterTable>
    <ParameterRow platform="js" name="channel" type="number">
        The channel number
    </ParameterRow>
    <ParameterRow platform="net" name="channel" type="double">
        The channel number
    </ParameterRow>
    <ParameterRow platform="android" name="channel" type="Double">
        The channel number
    </ParameterRow>
    <ParameterRow platform="js" name="solo" type="boolean">
        true if the channel should be played solo, otherwise false.
    </ParameterRow>
    <ParameterRow platform="net" name="solo" type="bool">
        true if the channel should be played solo, otherwise false.
    </ParameterRow>
    <ParameterRow platform="android" name="solo" type="Boolean">
        true if the channel should be played solo, otherwise false.
    </ParameterRow>
</ParameterTable>

### Returns 
Nothing 
