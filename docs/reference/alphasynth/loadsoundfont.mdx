---
title: loadSoundFont
description: Loads a soundfont from the given data
sidebar_custom_props:
  category: Methods
---
import { SinceBadge } from '@site/src/components/SinceBadge';


import {TypeTable, TypeRow} from '@site/src/components/TypeTable';
import {ParameterTable, ParameterRow} from '@site/src/components/ParameterTable';

## Description
Loads a soundfont from the given data 

## Signatures

<TypeTable>
    <TypeRow type="js" name="loadSoundFont(data: Uint8Array, append: boolean): void;" />
    <TypeRow type="net" name="void LoadSoundFont(Uint8Array data, bool append)" />
    <TypeRow type="android" name="fun loadSoundFont(data: Uint8Array, append: Boolean): Unit" />
</TypeTable>

### Parameters 

<ParameterTable>
    <ParameterRow platform="js" name="data" type="Uint8Array">
        a byte array to load the data from
    </ParameterRow>
    <ParameterRow platform="net" name="data" type="Uint8Array">
        a byte array to load the data from
    </ParameterRow>
    <ParameterRow platform="android" name="data" type="Uint8Array">
        a byte array to load the data from
    </ParameterRow>
    <ParameterRow platform="js" name="append" type="boolean">
        Whether to fully replace or append the data from the given soundfont.
    </ParameterRow>
    <ParameterRow platform="net" name="append" type="bool">
        Whether to fully replace or append the data from the given soundfont.
    </ParameterRow>
    <ParameterRow platform="android" name="append" type="Boolean">
        Whether to fully replace or append the data from the given soundfont.
    </ParameterRow>
</ParameterTable>

### Returns 
Nothing 
